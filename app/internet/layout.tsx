import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Internet Subscription | High Score VTU",
    description: "Subscribe to Smile, Spectranet, and Swift internet plans.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
