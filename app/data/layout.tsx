import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Buy Cheap Data | High Score VTU",
    description: "Purchase affordable mobile data bundles for MTN, Airtel, Glo, and 9mobile instantly.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
